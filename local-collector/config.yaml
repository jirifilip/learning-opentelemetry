receivers:
  prometheus:
    config:
      scrape_configs:
        - job_name: "node-exporter"
          static_configs:
            - targets:
                - "node-exporter-1:9100"
        - job_name: "cadvisor"
          static_configs:
            - targets:
              - "cadvisor:8080"


exporters:
  otlp_http:
    endpoint: "http://remote-opentelemetry-collector:4318"
    tls:
      insecure: true

service:
  pipelines:
    metrics:
      receivers: [prometheus]
      exporters: [otlp_http]
